[project]
name = 'picura-static'
version = '0.0.0'
description = 'Static site generator for a personal photo portfolio (concept).'
requires-python = '>=3.14'
dependencies = [
    'jinja2~=3.1',
    'pillow~=12.2.0',
    'pyyaml~=6.0',
]

[dependency-groups]
dev = [
    'pytest>=9',
]
